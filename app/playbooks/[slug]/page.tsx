import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { ArrowLeft, Check, GitBranch, TestTube2, TriangleAlert } from "lucide-react";
import { playbooks } from "@/data/playbooks";
type Params=Promise<{slug:string}>;
export async function generateMetadata({params}:{params:Params}):Promise<Metadata>{const {slug}=await params;const item=playbooks.find((entry)=>entry.slug===slug);return{title:item?.title??"Playbook"}}
export default async function PlaybookPage({params}:{params:Params}){const {slug}=await params;const playbook=playbooks.find((item)=>item.slug===slug);if(!playbook)notFound();return <article className="playbook-detail"><header className="playbook-hero container"><Link className="back-link" href="/playbooks"><ArrowLeft size={13}/> All playbooks</Link><span className="eyebrow">Engineering playbook</span><h1>{playbook.title}</h1><p>{playbook.summary}</p><div className="tag-row">{playbook.relatedTopics.map((topic)=><span className="tag tag-accent" key={topic}>{topic}</span>)}</div></header><div className="container playbook-layout"><aside><nav><a href="#problem">Problem</a><a href="#decision">Decision tree</a><a href="#pipeline">Pipeline</a><a href="#tradeoffs">Trade-offs</a><a href="#failures">Failure modes</a><a href="#experiments">Experiments</a></nav></aside><div><section id="problem"><span className="playbook-icon"><TriangleAlert size={17}/></span><h2>Problem</h2><p className="lead">{playbook.problem}</p></section><section id="decision"><span className="playbook-icon"><GitBranch size={17}/></span><h2>Decision tree</h2><ol className="decision-tree">{playbook.decisionTree.map((item,index)=><li key={item}><span>{String(index+1).padStart(2,"0")}</span><p>{item}</p></li>)}</ol></section><section id="pipeline"><span className="playbook-icon"><Check size={17}/></span><h2>Recommended pipeline</h2><div className="pipeline-track">{playbook.pipeline.map((item,index)=><div key={item}><span>{index+1}</span><strong>{item}</strong></div>)}</div></section><section id="tradeoffs"><h2>Trade-offs</h2><div className="tradeoff-table"><div className="tradeoff-head"><span>Choice</span><span>Use when</span><span>Primary risk</span></div>{playbook.tradeoffs.map((item)=><div key={item.choice}><strong>{item.choice}</strong><p>{item.when}</p><p>{item.risk}</p></div>)}</div></section><section id="failures"><span className="playbook-icon"><TriangleAlert size={17}/></span><h2>Failure modes</h2><ul className="failure-list">{playbook.failureModes.map((item)=><li key={item}>{item}</li>)}</ul></section><section id="experiments"><span className="playbook-icon"><TestTube2 size={17}/></span><h2>Recommended experiments</h2><ol className="experiment-list">{playbook.experiments.map((item,index)=><li key={item}><span>EXP-{String(index+1).padStart(2,"0")}</span><strong>{item}</strong></li>)}</ol></section></div></div></article>}
